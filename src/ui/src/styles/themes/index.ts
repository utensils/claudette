import type { ThemeDefinition } from "../../types/theme";
import defaultTheme from "./default.json";

export const BUILTIN_THEMES: ThemeDefinition[] = [defaultTheme as ThemeDefinition];

export const DEFAULT_THEME_ID = "default";
